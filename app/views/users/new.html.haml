%h1 Sign-up

= form_tag users_path do

  = label :user, :id, 'User-ID'
  = text_field :user, 'id'

  = label :E-Mail, :email, 'E-Mail'
  = text_field :user, 'email'

  = submit_tag 'Create my account'
